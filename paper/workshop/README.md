# Workshop paper -- QueryConditionedF (NeurIPS 2026 workshop, sept)

## Build

```bash
cd paper/workshop
pdflatex main.tex && bibtex main && pdflatex main.tex && pdflatex main.tex
```

Outputs `main.pdf`.

## Files

- `main.tex` -- root document, loads sections via `\input`.
- `section_*.tex` -- one file per section.
- `references.bib` -- BibTeX bibliography.
- `figures/` -- PDF figures generated by `python -m kiki_flow_core.track3_deploy.eval.kl_species`.
- `archive/section_*_v1.tex` -- pre-v2 drafts kept for history; not inputted by `main.tex`.

## Status

T22 (Phase 1 pilot 10k) and T23 (Phase 2 scale 50k) shipped 2026-04-20. Winner: `C_hash_mlp` (KL pilot10k=0.0785), weights at `kiki_flow_core/track3_deploy/weights/v0.3.safetensors`. Sections promoted to v2 (species reordering + citation updates) and made canonical 2026-05-10.
