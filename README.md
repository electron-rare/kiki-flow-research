# kiki-flow-core

Wasserstein-gradient-flow engine for micro-kiki self-organization.
See `docs/superpowers/specs/2026-04-17-kiki-flow-core-design.md` (in parent directory) for the design.

## Install
```bash
uv sync --all-extras
uv run pre-commit install
```

## Test
```bash
uv run pytest
```
